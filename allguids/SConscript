#############################################################################
# $Id$

Import('env')

sourceFiles = Split("""
   allguids.cpp
""")

local = env.Copy()
local.Append(CPPPATH=['#editor'])
local.BuildStaticLibrary(target='allguids',
                         source=sourceFiles)
