#############################################################################
# $Id$

Import('env')

sourceFiles = Split("""
   camera.cpp
   cmds.cpp
   engine.cpp
   entity.cpp
   entitycmds.cpp
   entitycmdmanager.cpp
   entitycmdui.cpp
   entitycomponents.cpp
   entitymanager.cpp
   geomipmap.cpp
   heightmap.cpp
   model.cpp
   modelanim.cpp
   modelms3d.cpp
   modelsgem.cpp
   modelskeleton.cpp
   modeltypes.cpp
   ms3dread.cpp
   saveloadmanager.cpp
   terrain.cpp
   terrainrender.cpp
   tixml.cpp
""")

libPaths = Split("""
   #allguids
   #gui
   #platform
   #rendergl
   #script
   #tech
""")

linkLibs = Split("""
   allguids
   gui
   platform
   rendergl
   script
   tech
""")

local = env.Copy()
local.UseGL()
local.UseTinyxml()
local.BuildLibrary(target='engine',
                   source=sourceFiles,
                   lib_path=libPaths,
                   libs=linkLibs)
